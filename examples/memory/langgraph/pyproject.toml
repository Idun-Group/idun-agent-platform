[project]
name = "idun-example-memory-langgraph"
version = "0.1.0"
description = "Idun Engine example: LangGraph chat with SQLite-backed conversation memory."
requires-python = ">=3.12,<3.13"
dependencies = [
    "idun-agent-engine>=0.6.2",
    "langgraph>=1.0",
    "langchain-google-genai>=2.0",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
