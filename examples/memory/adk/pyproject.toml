[project]
name = "idun-example-memory-adk"
version = "0.1.0"
description = "Idun Engine example: Google ADK chat with SQLite-backed session memory."
requires-python = ">=3.12,<3.13"
dependencies = [
    "idun-agent-engine>=0.6.2",
    "google-adk>=1.0",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
