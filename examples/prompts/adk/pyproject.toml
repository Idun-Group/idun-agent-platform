[project]
name = "idun-example-prompts-adk"
version = "0.1.0"
description = "Idun Engine example: ADK agent whose instruction is a managed prompt with Jinja2 variables."
requires-python = ">=3.12,<3.13"
dependencies = [
    "idun-agent-engine>=0.6.2",
    "google-adk>=1.0",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
