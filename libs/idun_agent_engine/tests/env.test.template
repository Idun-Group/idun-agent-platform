# =============================================================================
# Test Environment Configuration for idun_agent_engine
# =============================================================================
# Copy this file to .env.test and fill in the values for integration tests.
# Unit tests generally don't require these variables (mocks are used instead).
#
# IMPORTANT: Never commit actual credentials to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Langfuse Observability (for integration tests)
# -----------------------------------------------------------------------------
# These are only needed if running integration tests against a real Langfuse instance.
# For unit tests, observability is mocked or disabled.

LANGFUSE_PUBLIC_KEY=pk-test-xxxx
LANGFUSE_SECRET_KEY=sk-test-xxxx
LANGFUSE_BASE_URL=https://cloud.langfuse.com

# -----------------------------------------------------------------------------
# Phoenix Observability (for integration tests)
# -----------------------------------------------------------------------------
# These are only needed if running integration tests against a real Phoenix instance.

PHOENIX_API_KEY=test-api-key
PHOENIX_COLLECTOR_ENDPOINT=http://localhost:6006

# -----------------------------------------------------------------------------
# GCP Observability (for integration tests)
# -----------------------------------------------------------------------------
# These are only needed if running integration tests against GCP services.

GOOGLE_CLOUD_PROJECT=test-project-id
# GCP_TRACE_ENDPOINT=  # Optional: custom trace endpoint

# -----------------------------------------------------------------------------
# LLM Provider Keys (for integration tests)
# -----------------------------------------------------------------------------
# These are only needed if running integration tests that call real LLMs.
# Unit tests should mock LLM calls.

OPENAI_API_KEY=sk-test-xxxx
GOOGLE_API_KEY=test-api-key
ANTHROPIC_API_KEY=sk-ant-test-xxxx

# -----------------------------------------------------------------------------
# Database (for integration tests with checkpointer)
# -----------------------------------------------------------------------------
# SQLite is used by default for testing. For Postgres integration tests:

# POSTGRES_CONNECTION_STRING=postgresql://user:pass@localhost:5432/test_db

# -----------------------------------------------------------------------------
# MCP Servers (for integration tests)
# -----------------------------------------------------------------------------
# If testing MCP integrations, configure server endpoints here.

# MCP_TEST_SERVER_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Test Configuration
# -----------------------------------------------------------------------------
# Control test behavior

# Skip slow tests by default (override with: pytest -m "not slow")
# SKIP_SLOW_TESTS=1

# Enable verbose test output
# PYTEST_VERBOSE=1

# Run integration tests (by default, only unit tests run)
# RUN_INTEGRATION_TESTS=1
