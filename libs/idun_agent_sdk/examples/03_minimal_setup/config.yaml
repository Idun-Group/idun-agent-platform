# Minimal Configuration for Idun Agent SDK
# Just the essentials to get your agent running!

sdk:
  api:
    port: 8000

agent:
  type: "langgraph"
  config:
    name: "Minimal Agent"
    graph_definition: "minimal_agent.py:app" 