sdk:
  api:
    port: 8000
  telemetry:
    provider: "langfuse"

agent:
  type: "langgraph" 
  
  config:
    name: "My Example LangGraph Agent"
    graph_definition: "src/examples/example_agent.py:app"
    checkpointer:
      type: "sqlite"
      db_url: "sqlite:///example_checkpoint.db" 