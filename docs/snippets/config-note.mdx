<Note>
Configuration can be defined as a YAML file loaded during initialization (via `idun init`) or edited through the standalone admin panel at `/admin/`. See [Configuration](/configuration) for details.
</Note>
