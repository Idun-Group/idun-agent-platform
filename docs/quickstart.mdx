---
title: "Quickstart"
description: "Deploy your first AI agent with Idun in under 30 minutes — pip install to live chat UI."
keywords: ["quickstart", "getting started", "deploy agent", "LangGraph", "ADK", "idun-agent-engine", "pip install"]
---

`pip install` → chat UI → production agent in under 30 minutes. No Docker required, no separate frontend, no extra services to deploy.

## Prerequisites

- **Python 3.12+** ([python.org](https://www.python.org/downloads/))
- **pip** (bundled with Python)
- An LLM provider key (OpenAI, Anthropic, Google — whichever your agent calls)

## Steps

<Steps>
  <Step title="Install">
    ```bash
    pip install idun-agent-engine
    ```
    Installs the standalone package plus its dependencies (engine, FastAPI, Next.js static UI, AG-UI client).
  </Step>

  <Step title="Initialize a new agent project">
    ```bash
    idun init my-agent
    cd my-agent
    ```
    Scaffolds:
    - `agent.py` — minimal LangGraph agent template
    - `config.yaml` — engine + standalone config
    - `.env.example` — environment variables stub
  </Step>

  <Step title="Configure">
    Copy the env stub and add your LLM key:
    ```bash
    cp .env.example .env
    # Edit .env: set OPENAI_API_KEY (or your provider's key)
    ```
    Optionally review `config.yaml` to wire observability, guardrails, or MCP servers. The defaults work out of the box.
  </Step>

  <Step title="Serve">
    ```bash
    idun serve
    ```
    Runs Alembic migrations, seeds the database from `config.yaml`, and starts uvicorn at `http://localhost:8000`.

    ![Admin landing page](/images/readme/dashboard.png)
  </Step>

  <Step title="Chat">
    Open [http://localhost:8000](http://localhost:8000) in your browser. Send a message — the response streams back in real time.

    ![Chat conversation](/images/readme/chat.png)
  </Step>

  <Step title="Explore the admin">
    Open [http://localhost:8000/admin](http://localhost:8000/admin). View your agent's config, attach guardrails, register MCP tools, and inspect traces — all without redeploying.

    ![Agent detail](/images/readme/agent-detail.png)
  </Step>
</Steps>

## What next?

- [Pick a framework](/frameworks/overview) — LangGraph or Google ADK
- [Add guardrails](/guardrails/overview) — 15+ built-in safety guards
- [Wire observability](/observability/overview) — Langfuse, Phoenix, LangSmith, or GCP Trace
- [Govern MCP tools](/tool-governance/overview) — per-agent tool access control
- [Deploy](/deployment/overview) — Cloud Run, Kubernetes, or any single-container host

<Note>
  The original `docker-compose` and Manager UI quickstart paths were retired in v0.6. If you're upgrading from a pre-v0.6 install, file a [GitHub issue](https://github.com/Idun-Group/idun-agent-platform/issues/new?labels=docs%2Cmigration) and we'll publish a migration note.
</Note>
