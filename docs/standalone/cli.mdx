---
title: CLI reference
description: Every idun-standalone command, the flags it accepts, and what it does.
keywords: ["idun-standalone", "cli", "serve", "init", "hash-password", "export", "import", "db migrate"]
---

`idun-standalone` is the single entry point installed by the `idun-agent-standalone` package. All commands accept `--help` for inline documentation.

## serve

Run the standalone server. The DB is the source of truth in steady state; on first boot, if the DB is empty and `IDUN_CONFIG_PATH` points to a YAML file, the file seeds the DB.

```bash
idun-standalone serve
```

Flags (all optional — env vars take precedence when unset on the CLI):

| Flag | Env var | Default | Description |
|------|---------|---------|-------------|
| `--config <path>` | `IDUN_CONFIG_PATH` | `./config.yaml` | Bootstrap YAML file. |
| `--host <addr>` | `IDUN_HOST` | `127.0.0.1` | Bind address. Set `0.0.0.0` for containers (requires `IDUN_ADMIN_AUTH_MODE=password` or `IDUN_ALLOW_OPEN_ADMIN=1`). |
| `--port <int>` | `IDUN_PORT` | `8000` | Bind port. |
| `--auth-mode <mode>` | `IDUN_ADMIN_AUTH_MODE` | `none` | Admin gate. `oidc` is reserved for MVP-2. |
| `--ui-dir <path>` | `IDUN_UI_DIR` | bundled static export | Override the chat/admin UI directory. |
| `--database-url <url>` | `DATABASE_URL` | `sqlite+aiosqlite:///./idun.db` | SQLAlchemy URL. Use `postgresql+asyncpg://…` for Postgres. |
| (no flag) | `IDUN_ALLOW_OPEN_ADMIN` | `false` | Opt in to binding `0.0.0.0` with `auth_mode=none`. Containers and trusted networks only. |

## init

Scaffold a new agent project from the bundled template — a minimal LangGraph echo agent with a `config.yaml`, `.env.example`, `requirements.txt`, and a starter `README.md`.

```bash
idun-standalone init my-agent
cd my-agent
```

Flags:

- `--target <dir>` — write into a specific directory (defaults to `./<name>`).
- `--force` — overwrite a non-empty target directory.

## hash-password

Bcrypt the admin password so it can be stored in `IDUN_ADMIN_PASSWORD_HASH` (or a Secret Manager secret).

```bash
idun-standalone hash-password
```

Prompts for the password and prints the hash on stdout. The container image accepts the same hash via the `IDUN_ADMIN_PASSWORD_HASH` env var.

## export

Dump the current DB-backed config to YAML on stdout. Equivalent to `GET /admin/api/v1/config/export`.

```bash
idun-standalone export > my-config.yaml
```

Use this for backups, migrating to the [governance hub](/manager/overview), or diffing against the bootstrap YAML.

## import

Load a YAML file into the DB, replacing the current state. Mirrors the bootstrap flow but runs on demand.

```bash
idun-standalone import my-config.yaml
```

The next admin write triggers a hot reload of the running agent against the imported state.

## db migrate

Run Alembic migrations to head. Called automatically on `serve` startup; the standalone exposes the command directly so operators can pre-stage migrations during deploys.

```bash
idun-standalone db migrate
```

## Environment variables

Every flag has an env-var equivalent. Additional variables not exposed as flags:

| Variable | Default | Description |
|----------|---------|-------------|
| `IDUN_ADMIN_PASSWORD_HASH` | — | Bcrypt admin password hash. Consulted only on first boot to seed the admin row. Required when `auth_mode = password`. Generate with `idun hash-password`. |
| `IDUN_SESSION_SECRET` | — | At least 32 characters. Signs the `idun_session` cookie. Required when `auth_mode = password`; startup fails fast when shorter. |
| `IDUN_SESSION_TTL_HOURS` | `24` | Session cookie lifetime in hours (range `1..720`). |
| `IDUN_ALLOW_OPEN_ADMIN` | `false` | Opt-in flag that lets `auth_mode=none` bind `0.0.0.0` / `::`. Containers and trusted networks only. |
| `IDUN_TRACE_RETENTION_DAYS` | `14` | Days of trace events the daily retention task keeps before dropping. |
| `IDUN_TRACES_INPUT_VALUE_MAX_BYTES` | `65536` | Per-attribute byte cap before the exporter truncates trace input/output values. |
| `IDUN_PRICES_REFRESH` | `false` | When `true`, fetch the LiteLLM model-prices snapshot at boot (5 s timeout, snapshot fallback). |
